- name: play level variables
  hosts: local
  connection: local
  tasks:
    - name: Print variables
      vars:
        course: "Ansible"
      ansible.builtin.debug:
        msg: "Course is {{ course }}"    